#include <iostream>

int main(void) {
    int in1;
    std::cin >> in1;

    int in2;
    std::cin >> in2;

    int result = in1 + in2;
    std::cout << in1 << "+" << in2 << "=" << result << std::endl;

    return 0;
}